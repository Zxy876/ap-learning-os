# Blackboard Output Directory

This directory stores generated blackboard markdown such as `today.md`.

Generated blackboard files are ignored by git because they change every day and reflect local task state. The blackboard server source code is in `../blackboard_server.py`.

